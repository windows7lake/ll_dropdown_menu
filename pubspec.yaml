name: ll_dropdown_menu
description: DropDownMenu
version: 0.1.0
homepage: https://github.com/windows7lake/ll_dropdown_menu

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
